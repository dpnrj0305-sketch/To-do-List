# To-do-List
A lightweight, command-line To-Do List application built entirely with Python's standard libraries. It uses sqlite3 for persistent database storage and argparse for terminal interaction. Easily add, view, complete, and delete tasks directly from your CLI without needing complex setups or external dependencies. Fast and practical!
